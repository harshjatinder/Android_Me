package `in`.dev_op.androidme.ui

/**
 * Created by harsh on 31/12/17.
 * To handle click events on image item of grid view
 */
interface OnImageClickListener {
    fun onImageSelected(position: Int)
}